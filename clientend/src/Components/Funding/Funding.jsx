import { AppBar, Toolbar, Typography } from "@mui/material";

const Funding = () => {
    return (
        <AppBar position="static" color="primary">
            <Toolbar>
                <Typography variant="h6" component='div'>
                    Support Blood Donation Initiatives
                </Typography>
            </Toolbar>
        </AppBar>
    );
};

export default Funding;